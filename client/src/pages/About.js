import React from 'react';
import { Link } from 'react-router-dom';

const About = () => {
    return (
        <div>
            <h1>ABOUT</h1>
            <Link to={'/'}>home</Link>
        </div>
    );
};

export default About;